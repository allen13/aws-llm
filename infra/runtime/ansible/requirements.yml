---
# Ansible Galaxy collections. Installed by run.sh on tarball-etag change.
# Versions are loose-pinned by latest at time of writing; bump deliberately.
collections:
  - name: amazon.aws
    version: ">=8.0.0,<10.0.0"
  - name: community.aws
    version: ">=8.0.0,<10.0.0"
  - name: community.docker
    version: ">=3.10.0,<5.0.0"
  - name: community.general
    version: ">=9.0.0,<11.0.0"
  - name: ansible.posix
    version: ">=1.5.0,<3.0.0"
